# Reference Lakehouse TODO

- Add built-in managed examples for MySQL and MariaDB database sources alongside PostgreSQL and SQLite.
- Replace the direct Spark-submit reference workflow with Dagster asset orchestration once Dagster is part of the first-class runtime model.
- Model federated database access provisioning, including Vault-style dynamic credentials and policy-bound database roles.
- Promote the OpenMetadata OpenLineage ingestion job into a typed Datascape metadata operation when the metadata-operation contract is stable.
