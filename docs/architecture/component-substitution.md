# Component Substitution

Applications and domain contracts must depend on stable contracts, not vendor-specific runtime implementations.

Current release creates adapter and conformance package boundaries. Shared conformance suites are expanded in later planned releases.
