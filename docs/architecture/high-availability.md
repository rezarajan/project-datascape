# High Availability

HA is capability-driven. Compose is treated as local/integration unless a target capability genuinely supports HA.

The compiler rejects `production-ha` for Compose runtime profiles.
