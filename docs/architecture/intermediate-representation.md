# Intermediate Representation

The IR is a technology-neutral `PlatformPlan` containing stable logical resources, dependencies, source declarations, adapter ownership, canonical digests, rollout-sensitive digests, and recovery classification.

The IR is derived only after validation succeeds.
