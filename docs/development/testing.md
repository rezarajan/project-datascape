# Testing

Use the standard Go `testing` package.

Tests cover YAML/JSON parsing, validation, graph binding rules, external resources, policy overrides, capability resolution, canonicalization, hashing, deterministic compilation, selective Compose rendering, and change-isolation primitives.
