# Specification Versioning

The initial API version is `platform.datascape.dev/v1alpha1`.

Breaking changes require a new API version and migration guidance. Runtime registry IDs are not durable domain identifiers.
