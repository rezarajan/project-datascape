# Custom Runtime Profile

Runtime profiles select implementations for capabilities such as CDC, event bus, schema registry, application runtime, stream processing, table format, storage, catalog, orchestration, lineage, metadata, telemetry, and query.

Current release accepts profile resources and validates basic HA compatibility.
