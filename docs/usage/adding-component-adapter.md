# Adding a Component Adapter

Adapters declare capabilities and render target-owned artifacts from the normalized IR.

Do not import adapter packages into core domain packages. Add conformance checks for each swappable capability.
