# ADR-0039: Optional Architecture Patterns

Status: Accepted

Reference architectures are conveniences, not mandatory execution models. Patterns may expand into primitives and bindings, but direct resource declarations remain authoritative.

The compiler must produce equivalent graph output for equivalent pattern-expanded and hand-written resource graphs.
