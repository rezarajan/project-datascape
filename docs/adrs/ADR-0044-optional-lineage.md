# ADR-0044: Optional Lineage And lineage admission

Status: Accepted

Lineage is optional unless required by `PlatformPolicy` or selected through `Binding`. lineage admission is one lineage gateway mode, not the default architecture.

Direct OpenLineage-to-backend bindings are valid when selected and policy allows them.
