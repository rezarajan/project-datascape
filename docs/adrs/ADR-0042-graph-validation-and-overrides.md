# ADR-0042: Graph Validation And Overrides

Status: Accepted

The compiler validates graph correctness before rendering. Consumers require producer-backed, imported, or external streams. Producers do not require consumers.

Overrides must be declarative, named, scoped, and rendered in plan artifacts. Production overrides require policy permission and remediation text.
